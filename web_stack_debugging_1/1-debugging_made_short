#!/usr/bin/env bash
# Start nginx, check for HTTP 200, then stop nginx

service nginx start
curl -s -o /dev/null -w "%{http_code}" http://localhost/ | grep 200
service nginx stop
service nginx status | grep -q "inactive"
