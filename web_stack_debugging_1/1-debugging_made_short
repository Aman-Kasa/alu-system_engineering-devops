#!/usr/bin/env bash
# Starts nginx if not running and verifies HTTP 200 on port 80
if service nginx status | grep -q "inactive (dead)"; then
  service nginx start
fi
curl -s -o /dev/null -w "%{http_code}" http://localhost:80
