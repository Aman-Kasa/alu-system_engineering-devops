#!/usr/bin/env bash
# Start Nginx, verify HTTP 200 response, stop Nginx, check if inactive

sudo service nginx start
curl -s -o /dev/null -w "%{http_code}" http://localhost:80
sudo service nginx stop
sleep 1
sudo service nginx status | grep "inactive"
