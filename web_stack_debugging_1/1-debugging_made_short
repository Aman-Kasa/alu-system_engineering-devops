#!/usr/bin/env bash
# Starts nginx if not running and verifies HTTP 200 on port 80
service nginx status | grep -q 'inactive (dead)' && service nginx start
curl -s -o /dev/null -w "%{http_code}" http://localhost:80 | grep -q "200" && echo 1 || echo 0
