#!/usr/bin/env bash
# Start Nginx, check HTTP 200, stop Nginx, and check its status

service nginx start
curl -s http://localhost | grep "HTTP/1.1 200 OK"
service nginx stop
service nginx status | grep "inactive"
